# RCMD Environment Configuration Template
# Copy this file to .env and configure your settings

# Docker permissions (Linux/macOS only)
# Run 'id -u' and 'id -g' to get your user/group IDs
PUID=1000
PGID=1000

# Vite development server configuration
# Add domains/hosts that should be allowed to access the dev server
# Use comma-separated values, wildcards supported with dot prefix
# Examples:
#   VITE_ALLOWED_HOSTS=rcmd.example.com
#   VITE_ALLOWED_HOSTS=rcmd.example.com,.example.com,staging.myapp.com
#   VITE_ALLOWED_HOSTS=localhost,127.0.0.1,.local,.lan
VITE_ALLOWED_HOSTS=

# Database configuration (optional)
# Leave empty to use SQLite (default)
# Set to enable PostgreSQL
#POSTGRES_URL=postgresql://user:password@host:5432/database
#POSTGRES_HOST=localhost
#POSTGRES_USER=rcmd
#POSTGRES_PASSWORD=your_password
#POSTGRES_DB=rcmd

# Application settings
#NODE_ENV=development
#PORT=5173

# Timezone for Docker containers
#TZ=UTC